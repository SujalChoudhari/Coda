import Maths

// Its Done!
// Comments can be closed by a semi colon;
const int HOURS_CRIED = 30L; // this would run;
// this would also run; let float y = 40.0f;

// this is how you define functions
def function_name(param1, param2){
    // code goes brrrrrr.
    return 0;
}

// we have SCOPES!!
scope scopename {
    let string x = "Hello World";
    const int PI = 3.14f; // consts cannot be changed

    def fuc(param){
        return 3;
    }
}




// to use SCOPES
// we use the dot operator
println(scopename.x); // and yes semicolon is optional


// We have objects!!
let obj = {
    x: 30,
    y: 50,
    func: def(param1,param2){
        return Maths.add(param1, param2)
    }
}


// we have lists
const list abc = [1, 2, 4, 5];

// We have while loop
while (let x < 0) {
    x++;
}

// we have loops!
for (let i = 0; i < HOURS_CRIED; i++) {
    print("cry");
}
